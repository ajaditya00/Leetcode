class Solution {
public:
    int mySqrt(int x) {
    // double n = x / 2.0;
    // cout<<n;
    // while (abs(n * n - x) >0.1) {
    //     n= (n + x / n) / 2.0;
    //     cout<<n<<endl;
    // }
    // return n;
    // }
    return sqrt(x);
    }
};